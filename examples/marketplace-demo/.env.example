# Kaspa Network Configuration
KASPA_NETWORK=testnet
KASPA_RPC_URL=https://api-tn10.kaspa.org

# Service Registry
REGISTRY_URL=http://localhost:5000

# Orchestrator Configuration
ORCHESTRATOR_BUDGET_KAS=100
ORCHESTRATOR_WALLET_PATH=./wallets/orchestrator.json

# Optional: Private key (if not using wallet file)
# PRIVATE_KEY_WIF=

# Logging Configuration
# Options: debug (verbose), info (clean), warn, error
LOG_LEVEL=info
